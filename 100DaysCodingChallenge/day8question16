//Write a program to input three numbers and find the largest among them using if–else.
#include<stdio.h>
int main ()
{
	int a,b,c;
	printf("enter first number = ");
	scanf("%d",&a);
	printf("enter second number = ");
	scanf("%d",&b);
	printf("enter third number = ");
	scanf("%d",&c);
	
	if (a==b && c==b)
	{
		printf("all are equal");
	}
	else if (a>=b && a>=c)
	{
		printf("the larget number among these is = %d",a);
	}
	else if (b>=a && b>=c)
	{
		printf("the larget number among these is = %d",c);
	}
	else if (c>=b && c>=a)
	{
		printf("the larget number among these is = %d",c);
	}
	
	return 0;
}
